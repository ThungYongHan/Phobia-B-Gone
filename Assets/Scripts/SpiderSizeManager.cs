using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpiderSizeManager : MonoBehaviour
{
    public GameObject spiderPrefab;

    public Vector3 spiderVector;
    // Start is called before the first frame update
    void Start()
    {/*
        spiderPrefab = this.gameObject;
        spiderVector = spiderPrefab.transform.localScale;
        transform.localScale = new Vector3(0.04f,0.04f,0.04f);
        //spiderPrefab = transform.localScale;
        // Update is called once per frame
        void Update()
        {

        }*/
    }
}